name: CI

on:
  push:
    branches: [Modificaçoes_projetos_clientes]
    
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Instalar dependências
        run: npm install
      
      - name: Rodar testes
        run: npm test --coverage

